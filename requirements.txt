# Phase 1-3: detection, tracking, fine-tuning
ultralytics>=8.4.0
lap>=0.5.12          # ByteTrack association solver

# Phase 4: ONNX export + INT8 quantization
# Ultralytics auto-installs these on first export, but that path warns
# "restart runtime for updates to take effect" and can leave a half-initialised
# session. Pinning them keeps a fresh clone reproducible in one step.
onnx>=1.17.0
onnxruntime>=1.20.0
onnxslim>=0.1.59
